# 施工单 — 星空自动切换

> 不分期，本期一次性完成。

| 端 | 功能/字段 | 改动说明 | 涉及文件/组件 | 埋点/日志 | 备注 |
|---|----------|---------|-------------|----------|------|
| 前端 | 🔁 自动轮播按钮 | 底部栏右侧新增 🔁 按钮，默认灰色未选中态 | `App.js` (底部控制栏区域) | 统计开启/关闭次数 | 仅一个场景时置灰不可点 |
| 前端 | 自动轮播定时器 | 开启后每 N 分钟调用 `switchToNextScene()`；手动切换场景时重置 | `App.js` (useEffect/useRef 管理) | 记录每次自动切换 | 用 `setInterval` + `useRef` 保存 timer |
| 前端 | 间隔设置面板 | 长按 🔁 弹出面板，4 个预设选项 (15/30/45/60min)，点击即设置并关闭 | `App.js` (新增 Modal/Panel 状态) | 记录间隔修改 | 默认 30 分钟 |
| 前端 | 倒计时联动 | 倒计时归零时自动停止轮播，🔁 恢复未选中 | `App.js` (倒计时结束回调) | — | 调用 `clearInterval` |
| 前端 | 持久化 | 轮播开关状态 + 间隔值存 AsyncStorage | `App.js` (偏好存储) | — | 现有偏好存储逻辑沿用 |
