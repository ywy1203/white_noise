import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.study.companion',
  appName: '陪伴学习',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
